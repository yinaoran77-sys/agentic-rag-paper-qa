# 核心依赖 - Phase 3 数据库迁移所需
# 先装这些，LangChain 等重依赖后面再装

# FastAPI 核心
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# 配置管理
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# 数据库核心
sqlalchemy>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0
psycopg2-binary>=2.9.9

# Redis
redis>=5.0.0

# OpenSearch
opensearch-py>=2.5.0

# HTTP 客户端
httpx>=0.27.0
aiohttp>=3.9.0

# 工具库
tenacity>=8.3.0
orjson>=3.10.0
